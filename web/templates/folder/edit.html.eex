<%= link "Back", to: folder_path(@conn, :show, @folder) %>

<h2>Edit folder</h2>

<%= render "form.html", changeset: @changeset,
                        action: folder_path(@conn, :update, @folder) %>

